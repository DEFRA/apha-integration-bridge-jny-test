{
  "name": "apha-integration-bridge-jny-test",
  "version": "0.0.0",
  "type": "module",
  "description": "",
  "engines": {
    "node": ">=22.13.1"
  },
  "author": "Defra DDTS",
  "license": "OGL-UK-3.0",
  "aliases": {
    "~": ".",
    "page-objects": "test/page-objects",
    "components": "test/components"
  },
  "scripts": {
    "clean": "rm -rf allure-results && rm -rf allure-report && rm -f FAILED",
    "test": "npm run clean && npm run test:run",
    "test:run": "node ./bin/run-cucumber.mjs",
    "test:dev": "npm run clean && ENV_NAME=dev node ./bin/run-cucumber.mjs",
    "test:test": "npm run clean && ENV_NAME=test node ./bin/run-cucumber.mjs",
    "test:perf": "npm run clean && ENV_NAME=perf-test node ./bin/run-cucumber.mjs",
    "test:prod": "npm run clean && ENV_NAME=prod node ./bin/run-cucumber.mjs",
    "test:local": "npm run test",
    "test:github": "npm run test",
    "format": "prettier --write 'test/**/*.js' '**/*.{js,md,json,config.js}'",
    "format:check": "prettier --check 'test/**/*.js' '**/*.{js,md,json,config.js}'",
    "git:pre-commit-hook": "npm run format:check && npm run lint",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "postinstall": "npm run setup:husky",
    "setup:husky": "node -e \"try { (await import('husky')).default() } catch (e) { if (e.code !== 'ERR_MODULE_NOT_FOUND') throw e }\" --input-type module",
    "report": "node ./bin/generate-report.mjs",
    "report:publish": "npm run report && ./bin/publish-tests.sh"
  },
  "dependencies": {
    "@cucumber/cucumber": "10.9.0",
    "axios": "1.12.2",
    "chai": "5.2.0",
    "deepmerge": "4.3.1",
    "esm-module-alias": "2.2.1",
    "global-agent": "3.0.0",
    "undici": "7.22.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "9.1.0",
    "eslint-config-standard": "17.1.0",
    "eslint-plugin-prettier": "5.1.3",
    "husky": "9.1.7",
    "prettier": "3.4.2"
  }
}
